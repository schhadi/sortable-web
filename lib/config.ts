// ─────────────────────────────────────────────────────────────────────────────
// Placeholders that must be settled before this page is sent to anyone.
// See NOTES.md — these are deliberate blanks, not oversights.
// ─────────────────────────────────────────────────────────────────────────────

/** Working title only. Trademark availability has NOT been checked. */
export const BRAND = "Findable";

/** Stated as a fact in the note, never as a question. See NOTES.md. */
export const PRICE = "€79";

/**
 * Where a reply should go. Left empty on purpose: filling this in publishes an
 * address to every scraper that reads the page. Empty => the response block
 * offers "copy answers" only, and no mailto: link is rendered.
 */
export const RECIPIENT_EMAIL = "";

/**
 * Unused since the redesign: the masthead that printed it was replaced by a
 * top bar that carries the brand, the search shortcut and two anchors only.
 * Kept because the date is still a decision someone has to make if it returns.
 */
export const DATELINE = "Concept note · August 2026";
